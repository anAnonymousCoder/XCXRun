  <view class="number">
    <view class="calculate">\n{{meters}}km</view>
    </view>

<view class="mainView">
   <map
        class="mapView"
        style="width: 100%; height: 400rpx;"
        latitude="{{latitude}}"
        longitude="{{longitude}}"
        markers="{{markers}}"
        covers="{{covers}}"
        polyline="{{polyline}}"
 show-location style="width: 100%; height: 260px;"
        >
    </map>
</view>

    <view class="numbert">
  <text class="calculatet">\n时间：{{Time}}</text>
  </view>



<view class='run-bottom'>
          <view class='run-btn'  bindtap='startRun'>
              开始
          </view>
          <view class='run-btn1'  bindtap='stopRun'>
              暂停
          </view>
          <view class='run-btn2'  bindtap='over'>
              结束
          </view>
      </view>